import { CartProducts } from '@/app/components/CartProducts'

const CartPage = () => {
  return (
    <main
      className='animate-enter'
    >
      <CartProducts />
    </main>
  )
}

export default CartPage
